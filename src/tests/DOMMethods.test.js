/**
 * @jest-environment jsdom
 */

test("User can make attacks by clicking one of the buttons", () => {
  // simulate button click and check to make sure a function gets called.
  // On second thought, maybe write code for gameloop after all.
});
