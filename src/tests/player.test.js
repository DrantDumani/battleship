// players will need to have some way to attach themselves to the gameboard
// players need to be capable of attacking enemy gameboards
// CPU players should be able to make random attacks.
//* As a bonus, CPU players should try to hit adjacent panels if they hit a ship

import createGameBoard from "../gameBoard";
import createPlayer from "../player";

test("CPU player makes random, non-repeated attacks on enemy gameboard", () => {});
